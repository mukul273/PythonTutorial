#Tuples are immutable lists
tuple1=('Maths', 'Physics','Chemistry', 'Computer Sci')
print(tuple1)

#tuple1[0] = 'Art' // error because these are non modifiable
print(tuple1)

#empty tuple - watch out
empty_tuple1 = {} #  a inbuilt method just like lists but unlike sets
empty_tuple2 = ()
